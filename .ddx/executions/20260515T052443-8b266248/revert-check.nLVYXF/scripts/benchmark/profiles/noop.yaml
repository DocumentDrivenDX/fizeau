# Noop profile — fixture used by adapter unit tests. Points at a non-routable
# base_url so accidental network use fails loudly. Never pick this profile
# for a real matrix run.
id: noop
provider:
  type: openai-compat
  model: noop-model
  base_url: http://127.0.0.1:1/noop
  api_key_env: DDX_BENCH_NOOP_KEY
pricing:
  input_usd_per_mtok: 0.0
  output_usd_per_mtok: 0.0
  cached_input_usd_per_mtok: 0.0
limits:
  max_output_tokens: 1024
  context_tokens: 8192
  rate_limit_rpm: 1
  rate_limit_tpm: 1
sampling:
  temperature: 0.0
  reasoning: ""
versioning:
  resolved_at: "2026-04-29"
  snapshot: "noop-fixture"
