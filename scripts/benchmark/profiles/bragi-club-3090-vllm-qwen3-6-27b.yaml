# Qwen3.6-27B via bragi-club-3090 vLLM with autoround quantization.
#
# STATUS: provisional — endpoint, model ID, and quant label must be verified
# via preflight before this lane is included in any sweep phase. If the host
# is unreachable or the model ID differs, update this file and record the
# verification command in scripts/benchmark/terminalbench-2-1-sweep.yaml.
#
# Known facts (2026-05-07):
#   Hardware: RTX 3090 (bragi-club host)
#   Runtime:  vLLM (OpenAI-compatible API)
#   Quant:    autoround (exact model variant TBD)
#   Endpoint: http://bragi-club:8000/v1 (default vLLM port; confirm before use)
#
# Qwen3 thinking-mode recommended sampling: T=0.6, top_p=0.95, top_k=20.
id: bragi-club-3090-vllm-qwen3-6-27b
provider:
  type: openai-compat
  model: "<qwen3.6-27b-autoround-model-id>"   # replace with verified /v1/models id
  base_url: "http://bragi-club:8000/v1"       # provisional; confirm via preflight
  api_key_env: VLLM_API_KEY
pricing:
  input_usd_per_mtok: 0.0
  output_usd_per_mtok: 0.0
  cached_input_usd_per_mtok: 0.0
limits:
  max_output_tokens: 32768
  context_tokens: 32768     # confirm actual loaded context length from /v1/models
  rate_limit_rpm: 60
  rate_limit_tpm: 100000
sampling:
  temperature: 0.6
  reasoning: low
  top_p: 0.95
  top_k: 20
versioning:
  resolved_at: "2026-05-07"
  snapshot: "pending-preflight"   # update after verifying endpoint and model id
