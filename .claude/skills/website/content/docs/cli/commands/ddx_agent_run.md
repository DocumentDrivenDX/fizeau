---
title: "agent run"
generated: true
---

## ddx agent run

Invoke an agent with a prompt

```
ddx agent run [flags]
```

### Options

```
      --arm stringArray      Comparison arm: harness:model:label (repeatable)
      --compare              Compare harnesses on the same prompt
      --effort string        Reasoning effort override; normally omit when using --profile
      --harness string       Harness name (default from config); use 'agent' for the embedded DDx agent
      --harnesses string     Comma-separated harnesses for quorum
  -h, --help                 help for run
      --json                 Output as JSON (alias for --output json-result)
      --keep-sandbox         Preserve worktrees after comparison
      --model string         Model override; normally omit when using --profile
      --output string        Output format: text (default), json-result, session-jsonl (default "text")
      --permissions string   Permission level: safe, supervised, unrestricted (overrides config)
      --post-run string      Command to run in each worktree after the agent completes
      --profile string       Routing intent: default, cheap, fast, smart (selects harness, model, and defaults automatically)
      --project string       Project root path (default: CWD git root). Env: DDX_PROJECT_ROOT
      --prompt string        Path to prompt file
      --quorum string        Quorum strategy: any, majority, unanimous
      --record               Record prompt→response pair for virtual harness replay
      --sandbox              Run each comparison arm in an isolated git worktree
      --text string          Inline prompt text
      --timeout string       Timeout duration (e.g. 30s, 5m)
      --worktree string      Create/reuse a git worktree for the run
```

### Options inherited from parent commands

```
      --config string              config file (default is $HOME/.ddx.yml)
      --library-base-path string   override path for DDx library location
  -v, --verbose                    verbose output
```

### SEE ALSO

* [ddx agent](/docs/cli/commands/ddx_agent/)	 - Invoke AI agents with harness dispatch, quorum, and session logging

###### Auto generated by spf13/cobra on 21-Apr-2026
