# GraphQL perf baseline — 2026-04-23T00:38:25Z

Generated by the `cli/internal/server/perf` harness (ddx-9ce6842a).

## Fixture

- Projects: 10
- Beads per project: 500
- Total beads: 5000
- Agent sessions per project: 100

## Environment

`go=go1.26.2 goos=linux goarch=arm64 cpu-cores=12`

## Results (milliseconds)

| Target | iter | in-proc p50 | in-proc p95 | in-proc p99 | HTTP p50 | HTTP p95 | HTTP p99 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `agentSessions` | 40 | 3.88 | 6.03 | 9.65 | 4.71 | 8.69 | 9.80 |
| `bead` | 40 | 6.05 | 6.72 | 8.32 | 6.08 | 6.80 | 7.62 |
| `beadsByProject` | 40 | 6.56 | 8.84 | 10.76 | 7.06 | 9.29 | 10.26 |
| `beadsByProject_statusOpen` | 40 | 5.96 | 6.45 | 6.87 | 6.28 | 6.87 | 7.02 |
| `beads_crossProject` | 40 | 59.11 | 62.16 | 64.14 | 68.09 | 75.80 | 82.40 |
| `docGraph` | 40 | 0.12 | 0.57 | 3.89 | 0.12 | 0.24 | 2.36 |

## Targets covered

- `bead(id:)` — single-bead detail (shared with ddx-ad0db8fd).
- `beadsByProject` — scoped list, default filter.
- `beadsByProject_statusOpen` — scoped list with status filter pushdown.
- `beads` (cross-project) — all-projects feed; worst-case scan.
- `docGraph` — workspace doc graph build.
- `agentSessions` — sessions.jsonl aggregation (shared with ddx-2ceb02fa).

## Thresholds (ddx-9ce6842a AC §5)

Targets on the default fixture (10 projects × 500 beads = 5,000 beads total):

- `bead(id:)` p95 ≤ 50 ms in-process / ≤ 200 ms HTTP.
- `beadsByProject(first=50)` p95 ≤ 75 ms in-process / ≤ 250 ms HTTP.
- `beads(first=50)` p95 ≤ 200 ms in-process / ≤ 500 ms HTTP.

If the scope-fix + filter-pushdown (Part 2 steps 1 and 2) do not meet these,
the next step is the lightweight in-memory index (Part 2 step 3). The 2026-04-22
user directive also keeps a real data store (axon / sqlite / postgres) on the
table — that decision is raised to the user, not chosen unilaterally.

## CI posture

`make bench-graphql` regenerates this report. A +25% p95 regression gate
is proposed as a follow-up bead — the harness records thresholds here and
in the JSON sibling so that gate can compare generations without re-running
the full matrix against live code.
